# Environments
venv/
.venv/

# Python
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# Regenerated by `python src/run_pipeline.py` in ~15s (17MB of intermediates).
# Figures, metrics.json and the case list are committed because the README
# links to them and a reader should see the results without running anything.
outputs/transactions_clean.csv
outputs/transactions_scored.csv
